mkdir -p data
# move your downloaded file into the repo’s data folder:
# (on mac/windows, you can drag-drop; or if in terminal:)
# mv /path/to/download/catalog.json data/catalog.json
git add data/catalog.json
git commit -m "Add product catalog"
git push
